extends ../layout/layout

block head-title
  title Error - #{appName}

block content

  .page-title
    h1 Error

  .section.error-report
    if err.code
      | #{'Code: ' + err.code + '\n'}
    if err.stack
      | #{err.stack}
